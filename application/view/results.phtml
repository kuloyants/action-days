<?php
$tabs = $data['tabs'];
$activeTab =$data['activeTab'];
?>
<section id="results">
    <div class="container text-center wow fadeIn animated">
        <h2><?=translate('results.title')?></h2>
        <hr class="colored">
        <div class="row content-row">
            <div class="col-lg-12">
                <div class="portfolio-filter">
                    <ul id="filters" class="clearfix">
                        <? foreach ($tabs as $name => $tab) : ?>
                            <li>
                                <a href="?section=results&tab=<?=$name?>">
                                    <span class="filter<?=$activeTab == $name ? ' active' : ''?>"
                                          data-filter="<?=$name?>"><?=translate("tab.$name")?></span>
                                </a>
                            </li>
                        <? endforeach ?>
                    </ul>
                </div>
            </div>
        </div>
        <div class="content">
            <? $data = $tabs[$activeTab]?>
            <? include("results/{$activeTab}.phtml") ?>
        </div>
    </div>
</section>
<script>
    function refresh() {
        window.location.reload(true);
    }
    setTimeout(refresh, 30000);
</script>
